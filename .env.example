# Visual Report Builder Configuration Example
# All features are DISABLED by default - enable only what you need

# ==========================================
# 1. AUTHENTICATION (disabled by default)
# ==========================================
# Only enable if your project uses authentication
# VISUAL_REPORT_AUTH_ENABLED=false
# VISUAL_REPORT_WEB_MIDDLEWARE=         (empty = public)
# VISUAL_REPORT_API_MIDDLEWARE=         (empty = public)

# To enable authentication:
# VISUAL_REPORT_AUTH_ENABLED=true
# VISUAL_REPORT_WEB_MIDDLEWARE=auth
# VISUAL_REPORT_API_MIDDLEWARE=auth:sanctum

# ==========================================
# 2. MODELS (disabled by default)
# ==========================================
# Only enable if your project has these models
# VISUAL_REPORT_USER_MODEL=null         (no user model assumed)
# VISUAL_REPORT_ROLE_MODEL=null         (no role model assumed)
# VISUAL_REPORT_AUTO_DISCOVER=false     (no auto-discovery)

# To enable models (if they exist in your project):
# VISUAL_REPORT_USER_MODEL=App\Models\User
# VISUAL_REPORT_ROLE_MODEL=App\Models\Role
# VISUAL_REPORT_AUTO_DISCOVER=true

# ==========================================
# 3. CACHING (disabled by default)
# ==========================================
# Disabled by default to avoid cache issues
# VISUAL_REPORT_CACHE_ENABLED=false
# VISUAL_REPORT_CACHE_TTL=3600

# ==========================================
# 4. EXPORT CONFIGURATION
# ==========================================
VISUAL_REPORT_EXPORT_DISK=local
VISUAL_REPORT_EXPORT_PATH=exports
VISUAL_REPORT_MAX_EXPORT_ROWS=100000

# ==========================================
# 5. PERMISSIONS
# ==========================================
VISUAL_REPORT_CREATE_TEMPLATES=all
VISUAL_REPORT_CREATE_REPORTS=all
VISUAL_REPORT_SHARE_REPORTS=all
VISUAL_REPORT_EXPORT_REPORTS=all

# ==========================================
# 6. SESSION (if using file sessions)
# ==========================================
# SESSION_DRIVER=file
# Alternative for distributed systems: SESSION_DRIVER=database
